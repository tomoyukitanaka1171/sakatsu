import 'package:flutter/material.dart';

class DiaryPageModel extends StatelessWidget {
  const DiaryPageModel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
